# Directives

This folder is intentionally empty.

Directives get added here the moment some part of the build — a content edit, a deploy step, anything else — becomes a repeatable workflow worth writing down. Until then, work on this project is one-off design/content judgment, not something a directive should encode.
